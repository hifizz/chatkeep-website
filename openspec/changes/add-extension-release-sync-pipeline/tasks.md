## 1. 数据模型与契约

- [x] 1.1 在网站仓库定义并实现发布同步 DTO 校验（请求/响应、产物明细、渠道 latest）。
- [x] 1.2 新增发布相关数据表与迁移（发布主表、产物明细、渠道 latest 指针）。
- [x] 1.3 增加 `releaseId` 唯一约束与关键查询索引。

## 2. 内部同步接口

- [x] 2.1 新增 `POST /api/internal/extension-release` 路由。
- [x] 2.2 实现 HMAC + 时间窗校验。
- [x] 2.3 实现幂等写入（重复 releaseId 返回已有记录）。
- [x] 2.4 成功写入后更新渠道 latest 信息并返回标准响应 DTO。

## 3. 页面读取与即时刷新

- [x] 3.1 提供 `getChannelLatest(channel)` 服务查询。
- [x] 3.2 安装页接入 stable 最新版本与下载链接读取。
- [x] 3.3 更新页（changelog）接入最新版本摘要信息。
- [x] 3.4 发布同步成功后触发 `revalidatePath` 或 `revalidateTag`。

## 4. 安全与可观测

- [x] 4.1 增加同步接口访问日志（不记录敏感密钥）。
- [x] 4.2 增加错误分类与告警（签名失败、幂等冲突、写库失败）。
- [x] 4.3 补充单元/集成测试（签名校验、幂等、latest 更新、页面读取）。

## 5. 部署准备与联调

- [ ] 5.1 配置 `RELEASE_SYNC_SECRET` 等生产环境变量。
- [ ] 5.2 执行数据库 migration 并验证索引生效。
- [ ] 5.3 与扩展仓库完成 dev/rc/stable 三渠道联调。
- [x] 5.4 准备回滚方案（冻结同步接口、回退 latest 指针、页面缓存回刷）。
