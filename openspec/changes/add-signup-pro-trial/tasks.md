## 1. 注册赠送 Trial 能力

- [x] 1.1 在 billing 域新增“注册送 trial”服务函数（30 天窗口、`monthly + trial` 默认值）。
- [x] 1.2 为试用发放逻辑增加幂等保护：重复触发不重复创建、不得覆盖已有有效订阅。
- [x] 1.3 在订阅读取链路加入 trial 过期判定，超时后自动降级为非 Pro。

## 2. 认证注册流程集成

- [x] 2.1 在 `src/lib/auth.ts` 的服务端注册成功后置钩子接入 trial 发放调用。
- [x] 2.2 处理后置钩子异常：不阻断注册成功，但记录可追踪日志。
- [x] 2.3 校准 `/profile` 与 `/api/rpc/me` 链路，确保新用户注册后立即反映 Pro trial 状态。

## 3. 测试与验证

- [x] 3.1 补充/更新单元测试：覆盖注册送 trial、重复触发幂等、trial 到期降级。
- [x] 3.2 运行 `pnpm lint` 与 `pnpm typecheck`。
- [ ] 3.3 手工验证：新用户注册即 Pro trial；将 trial 结束时间改为过去后可降级为 Free。
